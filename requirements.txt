# 依赖：推理/训练均使用仓库内 ultralytics/ 代码，无需额外安装 ultralytics 包
# 但需要基础依赖：
numpy>=1.24
opencv-python>=4.6
torch>=2.0
torchvision>=0.15
# Windows py311 下 dlib 可用 dlib-bin；大多数场景不需要 dlib
# pyyaml, pandas, matplotlib（ultralytics 内部依赖，按需安装）